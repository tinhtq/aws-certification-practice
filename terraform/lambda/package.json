{
  "name": "bedrock-lambda",
  "version": "1.0.0",
  "description": "Lambda function for AWS Bedrock integration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && zip -r bedrock_lambda.zip index.js node_modules package.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.414.0"
  }
}
